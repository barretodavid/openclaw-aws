{
  "name": "integration",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:run": "jest --config jest.config.ts --runInBand"
  },
  "devDependencies": {
    "shared": "workspace:*"
  }
}
