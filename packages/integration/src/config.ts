/** AWS region used for integration tests (isolated from production in ca-central-1). */
export const TEST_REGION = 'us-east-2';

/** Availability zone within the test region. */
export const TEST_AZ = 'us-east-2a';
