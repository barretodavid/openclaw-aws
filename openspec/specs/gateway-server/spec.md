# Gateway Server

This capability has been removed. All gateway server responsibilities (channel integrations, WebSocket endpoint, agent logic execution, instance sizing) have been absorbed into the agent-server spec as part of the single-server architecture consolidation.
